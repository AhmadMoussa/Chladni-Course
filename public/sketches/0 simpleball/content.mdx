Example of a simple ball bouncing in a box. 

mdx example content

```jsx
// Ball bouncing in a box

x = random(20, 380);
y = random(20, 380);
vx = 2;
vy = 2;
```

mdx example content

```jsx
// Ball bouncing in a box
```

mdx example content

```jsx
// Ball bouncing in a box
```